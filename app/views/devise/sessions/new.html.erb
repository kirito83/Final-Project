<div class="topnav1" id="myTopnav">
      <div class="right-nav">
        <a id='logo1' href="/" class="active"><b>EASE</b></a>
      </div>
      <div class="left-nav1">
        <% if signed_in? %>
        <%= link_to 'LISTE DES TOURNOIS', '/tournaments' %>
        <%= link_to 'CREATION TOURNOI', new_tournament_path %>
        <% end %>
        <a href="#news"><b>NEWS</b></a>
        <a href="#contact"><b>CONTACT</b></a>
        <a href="#about"><b>ABOUT</b></a>
      </div>
      <div class='end-nav1'>
        <% if signed_in? %>
        <li class="dropdown">
          <a href="#" class='dropdown-toggle' data-toggle="dropdown">Mon compte&nbsp;<b class="caret"></b></a>
          <ul class="dropdown-menu">
            <li><%= link_to "Profil", show_path %></li>
            <li class="divider">|</li>
            <li> <%= link_to "Déconnexion", '/sign_out', method: :delete%> </li>
          </ul>
        </li>
        <% else %>
        <%= link_to "INSCRIPTION", new_user_registration_path, class: 'sign_up' %>
        <%= link_to "CONNEXION", '/sign_in', class: 'sign_in' %>
        <% end %>
      </div>
    </div>
<div class='signup'>
  <h1>CONNEXION</h1>

  <%= form_for(resource, as: resource_name, url: sign_in_path) do |f| %>
  <div class="field">
    <%= f.label :email, class: 'label' %><br />
    <%= f.email_field :email, autofocus: true, autocomplete: "email" %>
  </div>

  <div class="field">
    <%= f.label 'Mot de passe', class: 'label' %><br />
    <%= f.password_field :password, autocomplete: "off" %>
  </div>

  <% if devise_mapping.rememberable? -%>
  <div class="field">
    <%= f.check_box :remember_me %>
    <%= f.label 'Se souvenir de moi', class: 'label' %>
  </div>
  <% end -%>

  <br>

  <div class="actions">
    <%= f.submit "Se connecter", class: 'btn' %>
  </div>
  <% end %>

  <%= render "devise/shared/links" %>
</div>